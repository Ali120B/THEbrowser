{
  "name": "the-browser",
  "version": "0.1.0",
  "private": true,
  "description": "Linux-first spatial browser built with Electron",
  "main": "src/main/index.js",
  "scripts": {
    "start": "electron .",
    "test": "node --test tests/*.test.js",
    "check": "node --check src/main/index.js && node --check src/main/browser-manager.js && node --check src/main/state-store.js && node --check src/preload/index.js && node --check src/renderer/app.js"
  },
  "devDependencies": {
    "electron": "^40.0.0"
  }
}
